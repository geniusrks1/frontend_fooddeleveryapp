import styles from '../styles/DeliveryInfo.module.css';

const DeliveryInfo = () => {
  return (
    <section className={styles.deliveryInfo}>
      <div>Delivery in 20–30 minutes</div>
    </section>
  );
};

export default DeliveryInfo;
